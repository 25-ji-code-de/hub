# SEKAI Hub 真实数据测试指南

## 测试步骤

### 1. 获取 Access Token

访问 https://sekai.nightcord.de5.net 并登录，然后在浏览器控制台执行：

```javascript
localStorage.getItem('access_token')
```

复制返回的 token。

### 2. 测试 API 端点

```bash
# 设置环境变量
export SEKAI_ACCESS_TOKEN="your_access_token_here"

# 测试获取统计数据
curl "https://api.nightcord.de5.net/user/stats" \
  -H "Authorization: Bearer $SEKAI_ACCESS_TOKEN" | jq .

# 测试获取成就列表
curl "https://api.nightcord.de5.net/user/achievements" \
  -H "Authorization: Bearer $SEKAI_ACCESS_TOKEN" | jq .

# 测试上报事件
curl -X POST "https://api.nightcord.de5.net/user/events" \
  -H "Authorization: Bearer $SEKAI_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"project":"nightcord","event_type":"message_sent"}' | jq .
```

### 3. 验证 Hub 页面

1. 访问 https://sekai.nightcord.de5.net
2. 点击"登录"并授权
3. 登录后应该看到：
   - 用户名显示正确
   - "今日数据"卡片显示真实统计（如果有数据）
   - "最近解锁"显示真实成就（如果有解锁的成就）

### 4. 测试数据上报

在浏览器控制台执行：

```javascript
// 导入 API 模块（如果需要）
const API = (await import('./assets/js/api.js')).default;

// 上报一个测试事件
await API.reportEvent('nightcord', 'message_sent', { test: true });

// 刷新页面，查看统计是否更新
location.reload();
```

## 预期结果

### 初次登录（无数据）

- **今日数据卡片：** 显示 0 条消息、0 分钟等
- **最近解锁：** 显示"暂无解锁的成就"

### 有数据后

- **今日数据卡片：** 显示真实的消息数、在线时长等
- **最近解锁：** 显示最近解锁的成就（最多 5 个）

## 数据结构示例

### 统计数据响应

```json
{
  "user_id": "user_xxx",
  "date": "2026-02-11",
  "stats": {
    "nightcord": {
      "messages_sent": "23",
      "online_minutes": "90"
    },
    "25ji": {
      "study_minutes": "135",
      "pomodoro_completed": "3"
    },
    "nako": {
      "nako_conversation": "12"
    }
  }
}
```

### 成就数据响应

```json
{
  "user_id": "user_xxx",
  "achievements": [
    {
      "id": "msg_1000",
      "name": "话痨",
      "description": "在 Nightcord 发送 1000 条消息",
      "icon": "💬",
      "project": "nightcord",
      "type": "stat",
      "progress": 23,
      "unlocked": false,
      "unlocked_at": null
    },
    {
      "id": "streak_7",
      "name": "连续学习 7 天",
      "description": "连续 7 天使用 25ji 作業風景",
      "icon": "🔥",
      "project": "25ji",
      "type": "streak",
      "progress": 7,
      "unlocked": true,
      "unlocked_at": 1707654321000
    }
  ]
}
```

## 故障排查

### 问题：显示"数据加载失败"

**可能原因：**
1. Access token 过期（1 小时有效期）
2. API 服务器错误
3. 网络问题

**解决方法：**
1. 退出登录并重新登录
2. 检查浏览器控制台的错误信息
3. 检查 API 服务器状态

### 问题：统计数据为 0

**可能原因：**
1. 今天还没有使用过任何项目
2. 数据还没有上报

**解决方法：**
1. 使用 nightcord 发送消息
2. 使用 25ji 完成番茄钟
3. 手动上报测试事件（见上方测试脚本）

### 问题：成就列表为空

**可能原因：**
1. 还没有解锁任何成就
2. 使用时长/次数不足

**解决方法：**
1. 继续使用 SEKAI 生态项目
2. 查看成就要求（在成就列表中）
3. 等待系统自动检查成就（每次数据上报时）

## 下一步

- [ ] 在 nightcord 中集成数据上报
- [ ] 在 25ji 中集成数据上报
- [ ] 添加本周趋势图表（Chart.js）
- [ ] 添加成就进度条
- [ ] 添加数据刷新按钮
